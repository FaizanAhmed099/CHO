// Global environment configuration for frontend pages
// Set the API base URL here. For local development, keep localhost; for prod, set your deployment API URL.
window.__API_BASE__ = window.__API_BASE__ || "http://localhost:5000";
